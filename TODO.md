# Todo list

- [ ] DO NOT pass around state. Pass around context!
- [ ] Smart priorty passing module shared by clients
- [x] Should a player receive priority from the game engine if they have no game actions? Or is that a quality of life improvement for game clients to handle?
- [x] Should we go through all steps of all phases in the game engine even if nothing is happening in them? Or is that a quality of life improvement for game clients to handle?
- [x] Implement basic lands
